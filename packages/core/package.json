{
  "name": "@tokenledger/core",
  "version": "0.6.0",
  "description": "AI unit-economics engine for TokenLedger: live LLM pricing from OpenRouter, models.dev, GitHub Copilot, and Vercel AI Gateway; token/image/embed/video lanes; credits plans; scenario projections.",
  "keywords": [
    "ai",
    "llm",
    "tokens",
    "pricing",
    "unit-economics",
    "cost",
    "credits",
    "embeddings",
    "openrouter",
    "models.dev",
    "github",
    "vercel"
  ],
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist || rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test \"test/*.test.ts\"",
    "prepublishOnly": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "mdev-sdk": "^0.1.0"
  }
}